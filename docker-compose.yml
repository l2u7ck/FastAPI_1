version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    command: uvicorn src.server:app --host 0.0.0.0 --port 8000
    environment:
      - FASTAPI_ENV=development